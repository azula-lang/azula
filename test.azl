struct Person {
    int age,
    bool alive,
};

func new_person(int age, bool alive): Person {
    return Person{3, true};
}

func main(): void {
    :Person p = new_person(5, true);
    printf("Age: %5d Alive: %5d", p);
}