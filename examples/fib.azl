func fib(int x): int {
    if(x == 0 or x == 1) {
        return x;
    }
    return fib(x-1) + fib(x-2);
}

func main(): void {
    int i = 0;
    while(i < 50) {
        print("Fibonacci", i, fib(i));
        i = i + 1;
    }
}
