package "examples";

import {
    "math"
}

// Example of using the math functions from
// the standard library
func main(): void {
    println("Exponent of 3:");
    println(math.exp(3.0));

    println("Floor of 3.009:");
    println(math.floor(3.009));

    println("Ceil of 3.009:");
    println(math.ceil(3.0009));
}
