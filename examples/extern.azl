// sqrt hooks into the external C function 'sqrt'
extern func sqrt(float x): float;

func main(): void {
	// Create a float called 'y' that stores the computed square root of 5.0
	float y = sqrt(5.0);

	// Print the result
	println(y);
}